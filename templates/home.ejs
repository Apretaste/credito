<div class="row">
	<div class="col s12">
		<h1>
			<span class="green-text">§<%= Number.parseFloat(credit).toFixed(2) %></span>
		</h1>

		<p>Use su crédito para comprar dentro de la app, o transfiera créditos a otros usuarios.</p>

		<% if(canTransfer) { %>
			<a class="waves-effect waves-light btn-small" href="#" onclick="apretaste.send({command:'CREDITO ENVIAR'})">Transferir</a>
		<% } %>

		<a class="waves-effect waves-light btn-small grey" href="#" onclick="apretaste.send({command:'CREDITO OBTENER'})">Obtener crédito</a>

		<% if(items.length) { %>
			<h1>Transacciones</h1>
			<table class="striped"0>
				<% items.forEach(function(item){ %>
					<tr>
						<td>
							<%= item.type == 'purchase' ? "Canje" : (item.origin=='received' ? 'Recibido' : "Enviado") %><br/>
							<small class="nowrap text-gray"><%= item.datetime %></small>
						</td>
						<td><%= item.desc %></td>
						<td>
							<% if(item.origin == 'sent') { %>
								<span class="red-text">-§<%= Number.parseFloat(item.amount).toFixed(2) %>
							<% } else { %>
								<span class="green-text">+§<%= Number.parseFloat(item.amount).toFixed(2) %>
							<% } %>
						</td>
					</tr>
				<% }) %>
			</table>
		<% } %>
	</div>
</div>
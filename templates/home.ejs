<div class="row">
	<div class="col s12">
		<h1>
			<span class="green-text">§<%= Number.parseFloat(credit).toFixed(2) %></span>
		</h1>

		<p>Use su crédito para comprar dentro de la app, o transfiera créditos a otros usuarios.</p>

		<a class="waves-effect waves-light btn-small" href="#" onclick="apretaste.send({command:'CREDITO TRANSFERIR'})">Transferir</a>
		<a class="waves-effect waves-light btn-small grey" href="#" onclick="apretaste.send({command:'CREDITO OBTENER'})">Obtener crédito</a>

		<% if(items.length) { %>
			<h1>Transacciones</h1>

			<table class="striped"0>
				<% for(var i=0; i<items.length; i++) {
					var item = items[i]; %>
					<tr>
						<td><%= item.source == 'purchase' ? "Canje" : (item.type=='Recibido' ? 'Recibido' : "Enviado") %><br/>
						<span class="nowrap">
							<small style="color:gray;">
								<%= formatDate(item.transfer_time) %>
							</small>
						</span>
						</td>
						<td >
							<% if(item.type=='Enviado') { %>
								<%= item.source == 'purchase' ? item.name : 'Transferencia a @'+item.username %>
							<% } else { %>
								Recibido de @<%= item.sender_username %>
							<% } %>
						</td>
						<td <% if (item.type=='Enviado') { %> class="red-text" <%} else {%>class="green-text"<% } %>>
							<% if (item.type=='Enviado') { %> -<%} else {%> +<% } %>§<%= Number.parseFloat(item.amount).toFixed(2) %>
						</td>
						<td>
							<%= item.inventory_name %>
						</td>
					</tr>
				<% } %>
			</table>
		<% } %>
	</div>
</div>